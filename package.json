{
  "name": "client",
  "version": "1.0.0",
  "description": "offline client program to extract data from rdbms, convert to CSV files and generate appropriate metadata and configuration files.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.1",
    "express": "^4.15.2",
    "mysql": "^2.13.0"
  }
}
